# Mutation to create a new message
mutation CreateMessage($input: MessageInput!) {
  createMessage(input: $input) {
    code
    success
    message
    output {
      from
      to
      content
    }
  }
}
